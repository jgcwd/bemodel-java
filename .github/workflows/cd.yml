name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bemodel-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Docker deploy on ubuntu1
    runs-on: [self-hosted, ubuntu1]
    timeout-minutes: 30

    env:
      APP_DIR: /opt/bemodel-java
      FRONTEND_PORT: 18081
      MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE || 'bemodel_platform' }}
      MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
      MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
      MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      APP_SECRET_KEY: ${{ secrets.APP_SECRET_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Verify Docker
        shell: bash
        run: |
          set -euo pipefail
          docker --version
          docker compose version

      - name: Prepare deployment directory
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$APP_DIR"

      - name: Validate required secrets
        shell: bash
        run: |
          set -euo pipefail
          required=(MYSQL_USERNAME MYSQL_PASSWORD MYSQL_ROOT_PASSWORD DEEPSEEK_API_KEY JWT_SECRET APP_SECRET_KEY)
          for name in "${required[@]}"; do
            if [[ -z "${!name:-}" ]]; then
              echo "ERROR: GitHub Actions secret $name is not configured."
              exit 1
            fi
          done

      - name: Deploy with Docker Compose
        shell: bash
        run: |
          set -euo pipefail

          # The MySQL data lives in the named Docker volume bemodel_mysql_data.
          # Recreating containers does not delete the database volume.
          docker compose -f docker-compose.yml down --remove-orphans || true
          docker compose -f docker-compose.yml build --pull
          docker compose -f docker-compose.yml up -d --remove-orphans

      - name: Verify containers
        shell: bash
        run: |
          set -euo pipefail

          echo 'Container status:'
          docker compose -f docker-compose.yml ps

          echo 'Waiting for MySQL...'
          for i in {1..45}; do
            status=$(docker inspect --format '{{.State.Health.Status}}' bemodel-mysql 2>/dev/null || true)
            if [[ "$status" == "healthy" ]]; then
              break
            fi
            sleep 2
          done

          if [[ "$(docker inspect --format '{{.State.Health.Status}}' bemodel-mysql 2>/dev/null || true)" != "healthy" ]]; then
            echo 'MySQL did not become healthy.'
            docker compose -f docker-compose.yml logs --tail=150 mysql || true
            exit 1
          fi

          echo 'Waiting for backend...'
          for i in {1..45}; do
            if (echo > /dev/tcp/127.0.0.1/18080) >/dev/null 2>&1; then
              break
            fi
            sleep 2
          done

          if ! (echo > /dev/tcp/127.0.0.1/18080) >/dev/null 2>&1; then
            echo 'Backend did not become reachable.'
            docker compose -f docker-compose.yml logs --tail=150 backend || true
            exit 1
          fi

          echo 'Waiting for frontend...'
          for i in {1..20}; do
            if curl -fsS "http://127.0.0.1:${FRONTEND_PORT}/" >/dev/null 2>&1; then
              break
            fi
            sleep 1
          done

          curl -fsS "http://127.0.0.1:${FRONTEND_PORT}/" >/dev/null
          echo "Deployment successful: frontend :${FRONTEND_PORT}, backend :18080, mysql :3306"

      - name: Cleanup Docker resources
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          docker image prune -f
